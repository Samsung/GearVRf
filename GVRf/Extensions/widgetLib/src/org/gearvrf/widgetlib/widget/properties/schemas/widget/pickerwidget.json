{
  "$schema": "http://json-schema.org/schema#",
  "id": "pickerwidget.json",

  "properties": {
    "focus_animation": {
      "description": "Animation to run when an item in the picker gets line-of-sight focus",
      "$ref": "../common/animation.json"
    },
    "defocus_animation": {
      "description": "Animation to run when an item in the picker loses line-of-sight focus",
      "$ref": "../common/animation.json"
    }
  }
}